import { createUUID } from "./myFunc.js";
import { getCookie } from "./myFunc.js";

console.info("я на другом сайтек");
/*var userId = getCookie('serverUserId');
if (userId == undefined) {
    userId = createUUID();
    document.cookie = `serverUserId=${userId}`;
}*/
